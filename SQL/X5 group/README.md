# 🏪 Магазин "Пятерочка" - Система учета продаж

Проект представляет собой базу данных для управления товарами, продажами и инвентаризацией в магазине "Пятерочка".

## 📊 Структура базы данных

### Основные таблицы

#### 1. 🧑‍💼 Продавцы (sellers)
- Учет сотрудников магазина
- Информация о должностях и статусе активности

#### 2. 🛒 Товары (products)
- Каталог товаров магазина
- Цены, штрихкоды, категории и наличие

#### 3. 🧾 Чеки (receipts)
- Записи о кассовых операциях
- Информация о продавцах и способах оплаты

#### 4. 📝 Позиции чеков (receipt_items)
- Детализация покупок в чеках
- Количество, цены и общая стоимость

#### 5. 📦 Инвентаризация (inventory)
- Учет остатков товаров
- Контроль минимальных уровней запасов

### Связи многие-ко-многим

#### 6. 🗂️ Категории (categories)
- Иерархическая система категорий товаров
- Поддержка родительских и дочерних категорий

#### 7. 🔗 Связи товаров с категориями (product_categories)
- Промежуточная таблица для связи M:N
- Возможность назначения нескольких категорий одному товару


## 📊 Примеры запросов

### Топ продаваемых товаров
```sql
SELECT 
    p.product_name,
    SUM(ri.quantity) as total_quantity,
    SUM(ri.total_price) as total_revenue
FROM receipt_items ri
JOIN products p ON ri.product_id = p.product_id
GROUP BY p.product_name
ORDER BY total_revenue DESC;
```
### Продажи по кассирам
```sql
SELECT 
    s.first_name || ' ' || s.last_name as seller_name,
    COUNT(r.receipt_id) as receipts_count,
    SUM(r.total_amount) as total_sales
FROM receipts r
JOIN sellers s ON r.seller_id = s.seller_id
GROUP BY s.seller_id, seller_name
ORDER BY total_sales DESC;
```
### Товары для пополнения
```sql
SELECT 
    p.product_name,
    i.current_stock,
    i.min_stock_level
FROM inventory i
```
## 🚀 Установка и настройка

### Требования
- PostgreSQL 12+
- PgAdmin 4 v6+ (для просмотра ERD диаграмм)

### Инициализация базы данных
1. Создайте новую базу данных в PostgreSQL
2. Выполните SQL скрипт из файла `X5_group-project`
3. Для просмотра ERD диаграммы в PgAdmin:
   - откройте файл `ER_diagram.pgerd`

## 📈 Бизнес-возможности
- ✅ Анализ продаж и выручки
- ✅ Управление товарными запасами
- ✅ Контроль работы продавцов
- ✅ Гибкая категоризация товаров
- ✅ Отслеживание популярности товаров
- ✅ Автоматическое оповещение о низких запасах

## 🛠 Технические особенности
- **Нормализованная структура** - минимизация дублирования данных
- **Связи многие-ко-многим** - гибкая система категорий
- **Ограничения целостности** - внешние ключи и проверки
- **Автоматические расчеты** - подсчет сумм и остатков

## 📝 Примечания
- Проект разработан для демонстрации возможностей PostgreSQL
- Включает реалистичные тестовые данные для магазина "Пятерочка"
- ERD диаграмма создается средствами PgAdmin 4

## 👥 Автор
Шкодин Роман Владимирович
# Проект выполнен в рамках изучения баз данных и систем управления магазинами.

---
*Последнее обновление: 03.11.2025*
